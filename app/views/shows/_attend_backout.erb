<%= link_to_remote attendingshow?, {
	:url => { :action => "increment_attending", :id => show.id, :user_id => current_user.id, 
		:friends_attending => friends_attending, :youAreAttending => youAreAttending }}, 
		:class => "attendincrement", :title => "Click to Attend/Remove" %>